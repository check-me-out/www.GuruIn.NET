/*
define('centered-layout', ['jquery', 'utils'], function ($, utils) {

});
 */

    function init() {
        //Alertify('Testing custom popup message!', AlertSeverity.SUCCESS);
        //utils.Alertify('Hello, world!', AlertSeverity.ERROR);
        //utils.Alertify('Hello, world!');
    }

    RegisterOnload(init);
